# v1.14.0 — CardModal & Search Polish Spec

## Scope (4 items)

### 1. CardModal fwd/back nav (#77)
- Add prev/next navigation to CardModal when `context='search'`
- Chevrons in circles, no text — match deck card modal style exactly
- Navigate through search results array
- Hide buttons when ≤1 result (match deck modal behavior)

### 2. Set abbreviation click (#80)
- Make set code in CardModal clickable
- Hover affordance (underline or color change)
- Click → search `e:{setCode}`, switch to search view
- Works from both deck and search contexts

### 3. Artist name click (#81)
- Make artist name in CardModal clickable (if artist exists)
- Hover affordance matching set code
- Click → search `a:"{artistName}"`, switch to search view
- No link if artist field is missing/empty

### 4. Sort direction toggle
- Add asc/desc toggle button outside sort dropdown in search toolbar
- Mirror deck view sort direction toggle pattern
- Persist to localStorage key `mtg-search-sort-direction`
- Default: desc

## Files likely touched
- `CardModal.tsx` — nav buttons, clickable set/artist
- `SearchWorkspace.tsx` — sort direction state, toggle button
- `types/index.ts` — if search results array needs threading

## Housekeeping
- Update BACKLOG.md: check off #77, #80, #81, sort toggle item
- Update CLAUDE.md: version to v1.14.0, clear active milestone
- Update CHANGELOG.md: add v1.14.0 entry
